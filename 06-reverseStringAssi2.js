var string1="Hard work always pays back";

var reverse = "";

    for (let index = string1.length-1; index >=0 ; index--) {
        console.log(`The given string is: ${string1}`);
        
         reverse = reverse + string1.charAt(index);
    }
console.log(`The reverse string is:${reverse}`);

console.log("---------------------------------------------");
var string2="Soon I will be Angular IT Champ";
var reverse="";
 for (let index = string2.length-1; index >=0 ; index--) {
        console.log(`The given string is: ${string2}`);
         reverse = reverse + string2.charAt(index);
    }
console.log(`The reverse string is:${reverse}`);

console.log("---------------------------------------------");